spring:
  application:
    name: config-server
  cloud:
    config:
      server:
          git:
              uri: https://github.com/wu05281/springcloud
              searchPaths: file-config
              username: username
              password: password
server:
  port: 7001